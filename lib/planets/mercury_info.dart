import 'package:flutter/material.dart';

class MercuryInfo extends StatelessWidget {
  const MercuryInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('MERCURY'),
      ),
    );
  }
}